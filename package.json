{
  "name": "tokenbar",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "build:release": "tauri build && node scripts/collect-installers.mjs",
    "collect": "node scripts/collect-installers.mjs",
    "gen:noto": "node scripts/gen-noto-subset.mjs",
    "gen:playfair": "node scripts/gen-playfair-subset.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "html-to-image": "^1.11.13"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "jsdom": "^29.1.1",
    "subset-font": "^2.5.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
